<%# application.html.erbに共通部分を格納 %> 

<%# csvファイルを表示 %>

<div class="top">
  <h2>いまどうしてる？をつぶやこう</h2>
  <div class="tweets">
    <ul>
      <%# @tweetsの中身は配列なので変数tweetに入れて繰り返し処理 %>
      <% @tweets.each do |tweet| %>
      <li>
        <%= tweet[0] %>: <%= tweet[1] %>
        <span><%= tweet[2] %></span>
      </li>
      <% end %>
    </ul>
  </div>
</div>